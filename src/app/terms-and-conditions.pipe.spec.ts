import { TermsAndConditionsPipe } from './terms-and-conditions.pipe';

describe('TermsAndConditionsPipe', () => {
  it('create an instance', () => {
    const pipe = new TermsAndConditionsPipe();
    expect(pipe).toBeTruthy();
  });
});
